from setuptools import setup

setup(name="clef_dataloaders",
      version="0.2",
      description="Set of helper functions for loading CLEF data.",
      packages=["clef_dataloaders"],
      zip_safe=False,
      author="rlitschk",
      install_requires=["lxml"])